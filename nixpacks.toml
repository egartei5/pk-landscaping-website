[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
# `npm start` already runs: prisma migrate deploy -> scripts/bootstrap.mjs -> next start
cmd = "npm start"
